# SPDX-FileCopyrightText: Copyright 2026 Dash0 Inc.

groups:
  # ──────────────────────────────────────────────
  # dash0.genai.conversation.count
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.conversation.count
    type: metric
    metric_name: dash0.genai.conversation.count
    stability: stable
    brief: >
      Counts the number of GenAI conversations ingested by Dash0.
    note: |
      `dash0.genai.conversation.count` is a synthetic metric computed on-the-fly
      from the ingested span data.

      Deprecated PromQL alias: `dash0_genai_conversation_count_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{conversation}"

  - id: metric.dash0_genai_conversation_count_total
    type: metric
    metric_name: dash0_genai_conversation_count_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.conversation.count
    brief: >
      Deprecated Prometheus alias for `dash0.genai.conversation.count`.
    instrument: counter
    unit: "{conversation}"

  # ──────────────────────────────────────────────
  # dash0.genai.conversation.duration
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.conversation.duration
    type: metric
    metric_name: dash0.genai.conversation.duration
    stability: stable
    brief: >
      Distribution of GenAI conversation durations.
    note: |
      `dash0.genai.conversation.duration` is a synthetic metric computed
      on-the-fly from the ingested span data.

      Deprecated PromQL aliases: `dash0_genai_conversation_duration_seconds_bucket`,
      `dash0_genai_conversation_duration_seconds_sum`, `dash0_genai_conversation_duration_seconds_count`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: histogram
    unit: "s"

  - id: metric.dash0_genai_conversation_duration_seconds
    type: metric
    metric_name: dash0_genai_conversation_duration_seconds
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.conversation.duration
    brief: >
      Deprecated Prometheus alias for `dash0.genai.conversation.duration`.
    note: |
      Exposed in Prometheus as `dash0_genai_conversation_duration_seconds_bucket`,
      `dash0_genai_conversation_duration_seconds_sum`, and `dash0_genai_conversation_duration_seconds_count`.
    instrument: histogram
    unit: "s"

  # ──────────────────────────────────────────────
  # dash0.genai.conversation.errors
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.conversation.errors
    type: metric
    metric_name: dash0.genai.conversation.errors
    stability: stable
    brief: >
      Counts GenAI conversations that ended in an error.
    note: |
      `dash0.genai.conversation.errors` is a synthetic metric computed
      on-the-fly from the ingested span data.

      Deprecated PromQL alias: `dash0_genai_conversation_errors_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{error}"

  - id: metric.dash0_genai_conversation_errors_total
    type: metric
    metric_name: dash0_genai_conversation_errors_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.conversation.errors
    brief: >
      Deprecated Prometheus alias for `dash0.genai.conversation.errors`.
    instrument: counter
    unit: "{error}"

  # ──────────────────────────────────────────────
  # dash0.genai.chat.count
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.chat.count
    type: metric
    metric_name: dash0.genai.chat.count
    stability: stable
    brief: >
      Counts the number of GenAI chat operations ingested by Dash0.
    note: |
      `dash0.genai.chat.count` is a synthetic metric computed on-the-fly
      from the ingested span data.
      Each span with `gen_ai.operation.name=chat` contributes a single count.

      Deprecated PromQL alias: `dash0_genai_chat_count_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{chat}"

  - id: metric.dash0_genai_chat_count_total
    type: metric
    metric_name: dash0_genai_chat_count_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.chat.count
    brief: >
      Deprecated Prometheus alias for `dash0.genai.chat.count`.
    instrument: counter
    unit: "{chat}"

  # ──────────────────────────────────────────────
  # dash0.genai.chat.duration
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.chat.duration
    type: metric
    metric_name: dash0.genai.chat.duration
    stability: stable
    brief: >
      Distribution of GenAI chat operation durations.
    note: |
      `dash0.genai.chat.duration` is a synthetic metric computed on-the-fly
      from the ingested span data.
      The duration is the duration of each individual span with
      `gen_ai.operation.name=chat`.

      Deprecated PromQL aliases: `dash0_genai_chat_duration_seconds_bucket`,
      `dash0_genai_chat_duration_seconds_sum`, `dash0_genai_chat_duration_seconds_count`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: histogram
    unit: "s"

  - id: metric.dash0_genai_chat_duration_seconds
    type: metric
    metric_name: dash0_genai_chat_duration_seconds
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.chat.duration
    brief: >
      Deprecated Prometheus alias for `dash0.genai.chat.duration`.
    note: |
      Exposed in Prometheus as `dash0_genai_chat_duration_seconds_bucket`,
      `dash0_genai_chat_duration_seconds_sum`, and `dash0_genai_chat_duration_seconds_count`.
    instrument: histogram
    unit: "s"

  # ──────────────────────────────────────────────
  # dash0.genai.chat.errors
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.chat.errors
    type: metric
    metric_name: dash0.genai.chat.errors
    stability: stable
    brief: >
      Counts GenAI chat operations that ended in an error.
    note: |
      `dash0.genai.chat.errors` is a synthetic metric computed on-the-fly
      from the ingested span data.
      A span with `gen_ai.operation.name=chat` contributes when its status
      indicates an error.

      Deprecated PromQL alias: `dash0_genai_chat_errors_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{error}"

  - id: metric.dash0_genai_chat_errors_total
    type: metric
    metric_name: dash0_genai_chat_errors_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.chat.errors
    brief: >
      Deprecated Prometheus alias for `dash0.genai.chat.errors`.
    instrument: counter
    unit: "{error}"

  # ──────────────────────────────────────────────
  # dash0.genai.tool_call.count
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.tool_call.count
    type: metric
    metric_name: dash0.genai.tool_call.count
    stability: stable
    brief: >
      Counts the number of GenAI tool call operations ingested by Dash0.
    note: |
      `dash0.genai.tool_call.count` is a synthetic metric computed on-the-fly
      from the ingested span data.
      Each span with `gen_ai.operation.name=execute_tool` contributes a single count.

      Deprecated PromQL alias: `dash0_genai_tool_call_count_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{tool_call}"

  - id: metric.dash0_genai_tool_call_count_total
    type: metric
    metric_name: dash0_genai_tool_call_count_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.tool_call.count
    brief: >
      Deprecated Prometheus alias for `dash0.genai.tool_call.count`.
    instrument: counter
    unit: "{tool_call}"

  # ──────────────────────────────────────────────
  # dash0.genai.tool_call.duration
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.tool_call.duration
    type: metric
    metric_name: dash0.genai.tool_call.duration
    stability: stable
    brief: >
      Distribution of GenAI tool call operation durations.
    note: |
      `dash0.genai.tool_call.duration` is a synthetic metric computed on-the-fly
      from the ingested span data.
      The duration is the duration of each individual span with
      `gen_ai.operation.name=execute_tool`.

      Deprecated PromQL aliases: `dash0_genai_tool_call_duration_seconds_bucket`,
      `dash0_genai_tool_call_duration_seconds_sum`, `dash0_genai_tool_call_duration_seconds_count`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: histogram
    unit: "s"

  - id: metric.dash0_genai_tool_call_duration_seconds
    type: metric
    metric_name: dash0_genai_tool_call_duration_seconds
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.tool_call.duration
    brief: >
      Deprecated Prometheus alias for `dash0.genai.tool_call.duration`.
    note: |
      Exposed in Prometheus as `dash0_genai_tool_call_duration_seconds_bucket`,
      `dash0_genai_tool_call_duration_seconds_sum`, and `dash0_genai_tool_call_duration_seconds_count`.
    instrument: histogram
    unit: "s"

  # ──────────────────────────────────────────────
  # dash0.genai.tool_call.errors
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.tool_call.errors
    type: metric
    metric_name: dash0.genai.tool_call.errors
    stability: stable
    brief: >
      Counts GenAI tool call operations that ended in an error.
    note: |
      `dash0.genai.tool_call.errors` is a synthetic metric computed on-the-fly
      from the ingested span data.
      A span with `gen_ai.operation.name=execute_tool` contributes when its status
      indicates an error.

      Deprecated PromQL alias: `dash0_genai_tool_call_errors_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{error}"

  - id: metric.dash0_genai_tool_call_errors_total
    type: metric
    metric_name: dash0_genai_tool_call_errors_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.tool_call.errors
    brief: >
      Deprecated Prometheus alias for `dash0.genai.tool_call.errors`.
    instrument: counter
    unit: "{error}"

  # ──────────────────────────────────────────────
  # dash0.genai.subagent.count
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.subagent.count
    type: metric
    metric_name: dash0.genai.subagent.count
    stability: stable
    brief: >
      Counts the number of GenAI subagent invocations ingested by Dash0.
    note: |
      `dash0.genai.subagent.count` is a synthetic metric computed on-the-fly
      from the ingested span data.
      Each span with `gen_ai.operation.name=invoke_agent` contributes a single count.

      Deprecated PromQL alias: `dash0_genai_subagent_count_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{subagent}"

  - id: metric.dash0_genai_subagent_count_total
    type: metric
    metric_name: dash0_genai_subagent_count_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.subagent.count
    brief: >
      Deprecated Prometheus alias for `dash0.genai.subagent.count`.
    instrument: counter
    unit: "{subagent}"

  # ──────────────────────────────────────────────
  # dash0.genai.subagent.duration
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.subagent.duration
    type: metric
    metric_name: dash0.genai.subagent.duration
    stability: stable
    brief: >
      Distribution of GenAI subagent invocation durations.
    note: |
      `dash0.genai.subagent.duration` is a synthetic metric computed on-the-fly
      from the ingested span data.
      The duration is the duration of each individual span with
      `gen_ai.operation.name=invoke_agent`.

      Deprecated PromQL aliases: `dash0_genai_subagent_duration_seconds_bucket`,
      `dash0_genai_subagent_duration_seconds_sum`, `dash0_genai_subagent_duration_seconds_count`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: histogram
    unit: "s"

  - id: metric.dash0_genai_subagent_duration_seconds
    type: metric
    metric_name: dash0_genai_subagent_duration_seconds
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.subagent.duration
    brief: >
      Deprecated Prometheus alias for `dash0.genai.subagent.duration`.
    note: |
      Exposed in Prometheus as `dash0_genai_subagent_duration_seconds_bucket`,
      `dash0_genai_subagent_duration_seconds_sum`, and `dash0_genai_subagent_duration_seconds_count`.
    instrument: histogram
    unit: "s"

  # ──────────────────────────────────────────────
  # dash0.genai.subagent.errors
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.subagent.errors
    type: metric
    metric_name: dash0.genai.subagent.errors
    stability: stable
    brief: >
      Counts GenAI subagent invocations that ended in an error.
    note: |
      `dash0.genai.subagent.errors` is a synthetic metric computed on-the-fly
      from the ingested span data.
      A span with `gen_ai.operation.name=invoke_agent` contributes when its status
      indicates an error.

      Deprecated PromQL alias: `dash0_genai_subagent_errors_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{error}"

  - id: metric.dash0_genai_subagent_errors_total
    type: metric
    metric_name: dash0_genai_subagent_errors_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.subagent.errors
    brief: >
      Deprecated Prometheus alias for `dash0.genai.subagent.errors`.
    instrument: counter
    unit: "{error}"

  # ──────────────────────────────────────────────
  # dash0.genai.tokens.input
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.tokens.input
    type: metric
    metric_name: dash0.genai.tokens.input
    stability: stable
    brief: >
      Total GenAI input tokens consumed.
    note: |
      `dash0.genai.tokens.input` is a synthetic metric computed on-the-fly
      from the ingested span data.
      Each span contributes the value of its `gen_ai.usage.input_tokens`
      attribute to the per-bucket sum.

      Deprecated PromQL alias: `dash0_genai_tokens_input_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{token}"

  - id: metric.dash0_genai_tokens_input_total
    type: metric
    metric_name: dash0_genai_tokens_input_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.tokens.input
    brief: >
      Deprecated Prometheus alias for `dash0.genai.tokens.input`.
    instrument: counter
    unit: "{token}"

  # ──────────────────────────────────────────────
  # dash0.genai.tokens.output
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.tokens.output
    type: metric
    metric_name: dash0.genai.tokens.output
    stability: stable
    brief: >
      Total GenAI output tokens generated.
    note: |
      `dash0.genai.tokens.output` is a synthetic metric computed on-the-fly
      from the ingested span data.
      Each span contributes the value of its `gen_ai.usage.output_tokens`
      attribute to the per-bucket sum.

      Deprecated PromQL alias: `dash0_genai_tokens_output_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{token}"

  - id: metric.dash0_genai_tokens_output_total
    type: metric
    metric_name: dash0_genai_tokens_output_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.tokens.output
    brief: >
      Deprecated Prometheus alias for `dash0.genai.tokens.output`.
    instrument: counter
    unit: "{token}"

  # ──────────────────────────────────────────────
  # dash0.genai.cache_tokens.input
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.cache_tokens.input
    type: metric
    metric_name: dash0.genai.cache_tokens.input
    stability: stable
    brief: >
      Total GenAI input tokens served from cache.
    note: |
      `dash0.genai.cache_tokens.input` is a synthetic metric computed
      on-the-fly from the ingested span data.
      Each span contributes the value of its
      `gen_ai.usage.cache_read.input_tokens` attribute to the per-bucket
      sum.

      Deprecated PromQL alias: `dash0_genai_cache_tokens_input_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{token}"

  - id: metric.dash0_genai_cache_tokens_input_total
    type: metric
    metric_name: dash0_genai_cache_tokens_input_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.cache_tokens.input
    brief: >
      Deprecated Prometheus alias for `dash0.genai.cache_tokens.input`.
    instrument: counter
    unit: "{token}"

  # ──────────────────────────────────────────────
  # dash0.genai.cache_tokens.output
  # ──────────────────────────────────────────────
  - id: metric.dash0.genai.cache_tokens.output
    type: metric
    metric_name: dash0.genai.cache_tokens.output
    stability: stable
    brief: >
      Total GenAI output tokens served from cache.
    note: |
      `dash0.genai.cache_tokens.output` is a synthetic metric computed
      on-the-fly from the ingested span data.
      Each span contributes the value of its
      `gen_ai.usage.cache_read.output_tokens` attribute to the per-bucket
      sum.

      Deprecated PromQL alias: `dash0_genai_cache_tokens_output_total`.

      Synthetic metrics do not expose attributes by default. Attributes are
      materialised through PromQL aggregations.
    instrument: counter
    unit: "{token}"

  - id: metric.dash0_genai_cache_tokens_output_total
    type: metric
    metric_name: dash0_genai_cache_tokens_output_total
    stability: stable
    deprecated:
      reason: renamed
      renamed_to: dash0.genai.cache_tokens.output
    brief: >
      Deprecated Prometheus alias for `dash0.genai.cache_tokens.output`.
    instrument: counter
    unit: "{token}"
